"""Binnacle SQL migrations (yoyo-migrations).

Not imported at runtime as Python — these are data files read from disk by
``binnacle.adapters.postgres_store``. This ``__init__.py`` only marks the
directory as part of the ``binnacle`` package for packaging purposes.
"""
